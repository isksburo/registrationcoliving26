# PLAN — ПАЛАТА №6

## Фаза 1 — ядро (P0) ✅
- [x] `shared.js`: хранилище + театр зависаний + киоск
- [x] `admin.html`: таблица, тумблер «оплачено», экспорт CSV
- [x] `index.html`: выбор скина + тайный вход в админку
- [x] `start.command`: локальный сервер (127.0.0.1) + Chrome-киоск

## Фаза 2 — скины (P0) ✅
- [x] `registratura.html` — «Советская регистратура» (черновик-дизайн)
- [x] `inyerface.html` — «Анти-UX арт» (черновик-интерфейс)
- [x] `palata.html` — ФИНАЛ: дизайн от регистратуры + интерфейс-издевательства + новые пытки (регистр-саботаж, приёмные часы, талон-очередь, поиск-наоборот, капча-инверсия, счётная машина суммы)
- [x] Все пишут данные через `Palata.Store` (сначала сохранить, потом глючить)

## Фаза 3 — обкатка (P1) 🔄
- [x] Security-review (XSS, CSV-инъекции, лаунчер)
- [x] Сумма поддерживает сотни тысяч/миллионы (счётная машина, точное число)
- [x] Деплой на GitHub Pages для теста на Mac Mini
- [ ] Прогон `palata.html` на доке/трекпаде: правда ли проходится за ~3 мин
- [ ] Прогнать 5–10 тестовых записей → проверить CSV в Excel (кириллица)

## Фаза 4 — на месте (P1)
- [ ] Прорепетировать на Mac Mini с доком/трекпадом
- [ ] Решить про второй стол при потоке > ~25 чел.
- [ ] Бэкап: экспорт CSV в середине вечеринки, не только в конце

## Открытые вопросы
- Сколько гостей ждём за время приёма? (влияет на число станций)
- Кто отмечает факт оплаты — гость в форме или главбух в админке? (сейчас: главбух руками)
